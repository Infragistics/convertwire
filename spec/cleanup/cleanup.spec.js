describe('cleanup', function(){
	
	var path = require('path');
	var cleanup = require(path.resolve(__dirname, '../'));
	
	describe('clean', function(){
		
		it('replaces stray slashes in ordered lists', function(){
			var src = '*1\.*';
			var dest = 
		});
	});
});